import { PageHeader } from "@/components/page-header";

export default function SpotPage() {
  return (
    <PageHeader
      eyebrow="Acque"
      title="Spot"
      description="Spazio predisposto per catalogare laghi, fiumi, poste, note tecniche e risultati."
    />
  );
}
